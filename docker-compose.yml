version: '3.8'

services:
  nginx:
    build: .
    image: nginx:1.27.3
    container_name: portfolio-web
    volumes:
      - .:/usr/share/nginx/html
    networks:
      - tunnel

networks:
  tunnel:
    external: true
